<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.fengp.basic.module.nmt.dal.mysql.objectivemode.ObjectiveModeMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="listObjectiveMode" parameterType="Long" resultType="cn.fengp.basic.module.nmt.dal.dataobject.objectivemode.ObjectiveModeExDO">
        select t.*,t1.name as objective_name,t2.name as mode_name  from nmt_objective_mode t
        left join nmt_course_objective t1 on t.objective_id = t1.id
        left join nmt_evaluate_mode t2 on t.mode_id = t2.id where t.course_id = #{courseId} and t.deleted = 0
    </select>


</mapper>